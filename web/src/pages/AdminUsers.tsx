export function AdminUsers() {
  return (
    <div className="py-6" data-testid="admin-users-page">
      <h1 className="text-2xl font-semibold">Sysadmin · Users</h1>
      <p className="mt-3 text-zinc-600">
        Stub — wire to <span className="font-mono">GET /v1/users</span> (listUsers, ADR 0015) with
        substring search.
      </p>
    </div>
  );
}
